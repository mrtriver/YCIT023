const VOTING_AGE =18;

let canditateAge = 15;

let canVote = true;



//ternary if STATEMENT

//  canVote = canditateAge >= VOTING_AGE ? true : false;

 canVote = canditateAge >= VOTING_AGE   //exactly the same with above one
console.log(canVote);