myString = "apple pear quick pear lemon"
//found = myString.indexOf("pear");  gives first occurence
//found = myString.lastIndexOf("pear"); //gives last occcurence

//found = myString.startsWith("app"); gives boolean of starting

found = myString.endsWith("n");   //similar to startWith
console.log(found);   
